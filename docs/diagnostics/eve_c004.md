# EVE_C004: Node.js version unsupported

The app is running on a Node.js version older than 24.

## Fix

Install Node.js 24, run `pnpm install`, and restart the app. Vercel uses the version declared in `package.json` automatically.
